<%= form_for [@game_state, @module_hold] do |f| %>
  <% if @module_hold.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@module_hold.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @module_hold.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :build %><br>
    <%= f.radio_group name: :build, values: @blueprint_hold.build_options.map(&:to_json) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
